[project]
name = "prosthetic-hand-controller"
version = "0.1.0"
description = "Grasp taxonomy, proportional myoelectric control, and a simulated force feedback loop."
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
authors = [{ name = "Emre Yilmaz" }]
keywords = ["prosthetics", "grasp", "myoelectric-control", "robotic-hand", "python"]
dependencies = ["numpy>=2.0", "scipy>=1.14", "matplotlib>=3.9"]

[dependency-groups]
dev = ["pytest>=8.3", "ruff>=0.8", "mypy>=1.13"]

[project.optional-dependencies]
dev = ["pytest>=8.3", "ruff>=0.8", "mypy>=1.13"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hand_controller"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["slow: excluded from the default run"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src/hand_controller"]

# SciPy ships no type information. Two functions are imported from it:
# scipy.signal.butter and scipy.signal.sosfilt in the slip detector, and
# scipy.optimize.brentq in the grasp span solver. Each result is converted to a
# typed value at the call site.
[[tool.mypy.overrides]]
module = ["scipy.*"]
ignore_missing_imports = true
